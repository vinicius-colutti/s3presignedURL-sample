{
  "name": "presigneds3-server",
  "version": "1.0.0",
  "description": "app to calling s3 API and generate a pre signed URL, for upload specific files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vinicius Colutti",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1467.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
